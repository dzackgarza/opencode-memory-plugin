source_up
dotenv_if_exists .env

export OPENCODE_CONFIG="$PWD/.config/opencode.json"
export OPENCODE_CONFIG_DIR="$PWD/.config"
export OPENCODE_MEMORY_TEST_SEED="FMEM-42"

# Optional: override the memory root for local manual testing
# export OPENCODE_MEMORY_ROOT="$HOME/.local/share/opencode-memory/my-project"
